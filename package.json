{
  "name": "tp-2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "cd server/app && . ../.venv/bin/activate && uvicorn main:app --reload",
    "dev:frontend": "cd client && pnpm dev",
    "dev": "concurrently \"pnpm dev:backend\" \"pnpm dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
